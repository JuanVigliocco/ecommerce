import NavBar from '../Nav/NavBar';
import './header.css';
const Header = () => {
    return (
        <header className='contenedorheader'>
            <NavBar/>
        </header>
    );
};

export default Header